/**
 *
 * @param recipient Recipient publick key
 * @param token_uri Link to json file containing the metadata of the NFT
 */
async function createNft(recipient: string, token_uri: string) {
  try {
    
  } catch (e: any) {
    console.error(e);
  }
}
